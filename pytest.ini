[pytest]
testpaths = tests
addopts =
    -v
    --tb=short
    --html=reports/test_report.html
    --self-contained-html
markers =
    positive: Positive test cases (valid credentials)
    negative: Negative test cases (invalid / edge-case credentials)
